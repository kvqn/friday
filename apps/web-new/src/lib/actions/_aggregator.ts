"use server"

import { aggregator } from "../friday"

export const logs = aggregator.logs
export const count = aggregator.count
export const namespaces = aggregator.namespaces
export const topics = aggregator.topics
