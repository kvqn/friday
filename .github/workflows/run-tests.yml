name: Run Tests
run-name: Run Tests
on:
  push: {}
  workflow_dispatch: {}

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run Tests
        working-directory: ./tests
        run: ./run-tests.sh

